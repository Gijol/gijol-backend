spring:
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: course
      port: 27017
      host: localhost

# for query logging
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG

